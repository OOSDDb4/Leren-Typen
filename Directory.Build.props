<Project>
  <PropertyGroup Condition="'$(CI)' == 'true'">
    <!-- Override MAUI target frameworks so restore won't require workloads -->
    <TargetFrameworks>net8.0</TargetFrameworks>
  </PropertyGroup>
</Project>
